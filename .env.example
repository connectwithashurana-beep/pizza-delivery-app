# Django
DJANGO_SECRET_KEY=change-this-to-a-random-secret-key
# DEBUG now defaults to False in code if unset — set explicitly to True for local dev.
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# MySQL Database
DB_NAME=pizza_app
DB_USER=root
DB_PASSWORD=ashu
DB_HOST=localhost
DB_PORT=3306

# CORS / Frontend
CORS_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# Email (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password
ADMIN_EMAIL=admin@example.com

# Razorpay (Test Mode)
RAZORPAY_KEY_ID=rzp_test_TR9dDf44Va1Hc8 
RAZORPAY_KEY_SECRET=i6oggQEZppJYQPDlpiYUCJGA 

# Redis
REDIS_URL=redis://localhost:6379/0

# Business config
GST_PERCENT=5
DELIVERY_CHARGE=40

# Token expiry (added during audit — these were previously unenforced/unlimited)
EMAIL_VERIFICATION_TOKEN_EXPIRY_HOURS=24
PASSWORD_RESET_TOKEN_EXPIRY_MINUTES=30

# Frontend .env (create frontend/.env separately)
VITE_API_BASE_URL=http://localhost:8000/api
VITE_WS_BASE_URL=ws://localhost:8000/ws
VITE_RAZORPAY_KEY_ID=rzp_test_TR9dDf44Va1Hc8 
